---
permalink: /sitemap-filters.xml
sitemapIgnore: true
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
{%- for item in collections.shop %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.5</priority>
	</url>
{%- endfor %}
{%- for item in collections.style %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.5</priority>
	</url>
{%- endfor %}
{%- for item in collections.styleMain %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.5</priority>
	</url>
{%- endfor %}
{%- for item in collections.breweryStyle %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.3</priority>
	</url>
{%- endfor %}
{%- for item in collections.breweryShop %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.3</priority>
	</url>
{%- endfor %}
{%- for item in collections.styleShop %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.3</priority>
	</url>
{%- endfor %}
{%- for item in collections.breweryStyleShop %}
	<url>
		<loc>{{ meta.site.url }}{{ item.url }}</loc>
		<lastmod>{{ item.date | dateISOShort }}</lastmod>
		<changefreq>monthly</changefreq>
		<priority>0.2</priority>
	</url>
{%- endfor %}
</urlset>
